# Confidential interview brief. Never commit.
TASK.md

# Python
__pycache__/
*.py[cod]
.venv/

# Tooling caches
.ruff_cache/

# Runtime data: the working database, WAL companions, and the learned
# selector fingerprint store. Scoped to data/ on purpose, so the seed database
# committed under seed/ at step 11 is not swept up by a blanket *.db rule.
data/
*.db-wal
*.db-shm

# Local configuration
.env
